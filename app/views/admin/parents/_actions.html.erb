<td><%= link_to t('.show'), admin_parent_path(parent.id) %></td>
<td><%= link_to t('.edit'), edit_admin_parent_path(parent.id) %></td>
<td><%= link_to t('.delete'), admin_parent_path(parent.id), method: :delete, data: { confirm: t('.are_you_sure') } %></td>