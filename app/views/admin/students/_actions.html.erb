<td><%= link_to t('.show'), admin_student_path(student.id) %></td>
<td><%= link_to t('.edit'), edit_admin_student_path(student.id) %></td>
<td><%= link_to t('.delete'), admin_student_path(student.id), method: :delete, data: { confirm: t('.are_you_sure') } %></td>