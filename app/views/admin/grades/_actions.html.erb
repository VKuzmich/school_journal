<td><%= link_to t('.show'), admin_grade_path(grade.id) %></td>
<td><%= link_to t('.edit'), edit_admin_grade_path(grade.id) %></td>
<td><%= link_to t('.delete'), admin_grade_path(grade.id), method: :delete, data: { confirm: t('.are_you_sure') } %></td>
