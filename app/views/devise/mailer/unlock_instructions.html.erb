<p><%= (t 'devise.mailer.unlock_instructions.hello')%><%= @resource.email %>!</p>

<p><%= (t 'devise.mailer.unlock_instructions.your_account_locked') %></p>

<p><%= (t 'devise.mailer.unlock_instructions.click_link_to_unlock') %></p>

<p><%= link_to (t 'devise.mailer.unlock_instructions.link_to_unlock'), unlock_url(@resource, unlock_token: @token) %></p>
